import React, { Fragment } from 'react';

const StartMessage = () => {
  return (
    <Fragment>
      <h2 className="start-msg">Bienvenido</h2>
      <p className="start-msg">Use los deslizadores para comenzar</p>
    </Fragment>
  );
};

export default StartMessage;
